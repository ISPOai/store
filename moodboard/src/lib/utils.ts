// Registry components (shadcn) import `cn` from "@/lib/utils". The scaffold
// ships the helper at "@/lib/cn", so this thin re-export keeps installed
// registry files resolving without editing them.
export { cn } from './cn'
