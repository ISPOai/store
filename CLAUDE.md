# Working in this repo

Read `AGENTS.md` — it is the source of truth for this repo's house rules, and
it points to `docs/adding-an-app.md`, the required pipeline for adding or
porting an app. This file is deliberately just a pointer so the rules exist in
exactly one place and cannot drift.
